{% extends 'base.html.twig' %}

{% block body %}

    <a href="{{ path('factorriesgo_index') }}"><button class="btn btn-primary">Volver</button></a><br>
    <h1>Nuevo factor de riesgo</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Crear" class="btn btn-success">
    {{ form_end(form) }}

{% endblock %}
