{% extends 'base.html.twig' %}

{% block content %}

    <!-- Main Content -->
         <h2 class="text-center"> Visitantes </h2>

         <form class="navbar-form navbar-right" role="search">
            <div class="form-group">
               <input type="text" class="form-control" placeholder="Buscar">
            </div>
            <button type="submit" class="btn btn-default">Aceptar</button>
         </form>

         <table class="table table-hover table-striped table-bordered">
            <thead>
            <tr>
                <th>N° Ficha</th> 
                <th>Nombre</th>
                <th>Apellido</th>
                <th>Documento</th>
                <th>Pais</th>
                <th>Provincia</th>
                <th>Acciones</th>

            </tr>
            </thead>

            <tbody>
                <tr class="active">
                    <td></td>
                    <td></td>
                    <td></td>
                    <td></td>
                    <td></td>
                    <td></td>
                    <td>
                        <a href="{{ path('altaExcelView') }}"><button type="button" class="btn btn-success">Improtar excel</button></a>
                        <a href="{{ path('altaVisitante') }}"><button type="button" class="btn btn-success">Agregar un visitante</button></a>
                    </td>
                </tr>
                {% for visitante in visitantes %}
                    <tr class="active">
                        <td width="75px"> visitante.ficha </td>
                        <td>{{ visitante.nombre }}</td>
                        <td>{{ visitante.apellido }}</td>
                        <td>{{ visitante.tipoDocumento }} {{ visitante.nroDocumento }}</td>
                        <td>{{ visitante.pais }}</td>
                        <td>{{ visitante.provincia }}</td>
                        <td class="text-center" style="width: 400px;">
                            <button type="button" class="btn btn-sm btn-success">Alta Registro</button>
                            <button type="button" class="btn btn-sm btn-primary">Ver Detalle</button>
                            <button type="button" class="btn btn-sm btn-warning">Modificar</button>
                            <button type="button" class="btn btn-sm btn-danger">Eliminar</button>
                        </td>
                    </tr>
                {% endfor %}
         </table>         

{% endblock %}
