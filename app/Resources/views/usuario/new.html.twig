{% extends 'base.html.twig' %}
{% block sidebar %}

{% endblock %}
{% block content %}
    <h1>Nuevo Usuario</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('usuario_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
